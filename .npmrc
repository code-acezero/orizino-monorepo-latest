# Always install optional dependencies (required for platform-native binaries
# like lightningcss-linux-x64-gnu used by Tailwind CSS v4 / @tailwindcss/postcss)
omit=
optional=true
legacy-peer-deps=true
